dependencies:
  espressif/esp_hosted:
    version: "*"
    rules:
    - if: target in [esp32p4]

  espressif/esp_wifi_remote:
    version: "*"
    rules:
    - if: target in [esp32p4]

  espressif/esp_h264:
    version: "^1.0.4"
    rules:
    - if: target in [esp32p4]

  idf:
    version: ">=6.0.0"